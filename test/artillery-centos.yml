config:
  target: "http://localhost:8091"
  tls:
    rejectUnauthorized: false
  http:
    timeout: 10
  phases:
    - duration: 10
      arrivalRate: 1000

scenarios:
  - flow:
      - get:
          url: "/"
